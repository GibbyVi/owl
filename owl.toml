[owl]
version = "0.10.0"

[project]
name = "owl"
version = "0.13.1"
entry = "code/main.mire"

[compiler]
cmd = "mire"

[cache]
max_units = 256
analysis_cache = true
compression = false

[build]
optimization = "release"
debug = false
